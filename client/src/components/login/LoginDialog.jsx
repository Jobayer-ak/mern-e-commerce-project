
import { Dialog } from '@mui/material';
import React from 'react';

const LoginDialog = () => {
    return (
        <Dialog open={true}>
            Hello
        </Dialog>
};

export default LoginDialog;